import funkin.backend.system.framerate.FramerateCounter;

// Keep a reference so the class is included when compiling the mod.
final __framerateCounterClass:Class<FramerateCounter> = FramerateCounter;
