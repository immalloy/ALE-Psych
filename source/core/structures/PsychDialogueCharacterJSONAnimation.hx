package core.structures;

typedef PsychDialogueCharacterJSONAnimation = {
    var anim:String;

    var loop_name:String;
    var loop_offsets:Array<Int>;

    var idle_name:String;
    var idle_offsets:Array<Int>;
}